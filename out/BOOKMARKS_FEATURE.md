# Bookmarks Feature

## Обзор

Реализована полная система закладок (bookmarks/favorites) для файлового менеджера с тремя удобными способами добавления.

## Что реализовано

### Backend (Rust)

1. **Структура данных** (`src-tauri/src/config.rs`):
   - Добавлен тип `Bookmark` с полями: id, name, path, created_at
   - Добавлено поле `bookmarks: Vec<Bookmark>` в `AppConfig`
   - Закладки сохраняются в JSON config файле (`~/.config/vfdir/config.json`)

2. **API команды** (`src-tauri/src/commands.rs`):
   - `get_bookmarks()` - получить все закладки
   - `add_bookmark(path, name?)` - добавить закладку (имя опционально)
   - `remove_bookmark(id)` - удалить закладку
   - `rename_bookmark(id, new_name)` - переименовать закладку

3. **Регистрация команд** (`src-tauri/src/lib.rs`):
   - Все команды зарегистрированы в Tauri invoke_handler

### Frontend (Vue/TypeScript)

1. **Типы** (`src/types/index.ts`):
   - Добавлен интерфейс `Bookmark`
   - Обновлен интерфейс `AppConfig` с полем bookmarks

2. **Composable** (`src/composables/useBookmarks.ts`):
   - Управление состоянием закладок
   - Методы для всех CRUD операций
   - Обработка ошибок и loading состояния

3. **UI интеграция**:
   - **Sidebar** (`src/components/Sidebar.vue`):
     - Отображение закладок в секции Favorites
     - Inline редактирование имени (двойной клик)
     - Удаление закладки (кнопка ✕ при наведении)

   - **Toolbar** (`src/components/Toolbar.vue`):
     - Кнопка-звездочка для добавления/удаления текущей папки
     - Визуальная индикация (⭐ золотая если в избранном, ☆ серая если нет)

   - **Context Menu** (`src/components/ContextMenu.vue`):
     - Пункт "Add to Favorites" при ПКМ на папке

## Как использовать

### 🌟 Способ 1: Кнопка-звездочка в Toolbar (рекомендуется)

**Самый быстрый способ** - для добавления текущей открытой папки:

1. Откройте папку, которую хотите добавить в избранное
2. Кликните на кнопку ☆ (пустая звезда) в тулбаре
3. Кнопка станет ⭐ (золотая звезда) - папка добавлена!
4. Повторный клик удалит из избранного

**Или используйте горячую клавишу Ctrl+D**

### 📌 Способ 2: Контекстное меню (для любой папки)

Добавить в избранное любую папку из списка файлов:

1. Кликните правой кнопкой мыши на папке
2. Выберите "Add to Favorites" ⭐
3. Папка добавлена в избранное!

### ⌨️ Способ 3: Горячая клавиша Ctrl+D

1. Откройте папку или выберите её
2. Нажмите `Ctrl+D`
3. Готово! Текущая папка добавлена/удалена из избранного

### Навигация по закладке

- Кликните на закладку в списке Favorites
- Файловый менеджер перейдет в эту папку

### Переименование закладки

- Дважды кликните на имя закладки
- Введите новое имя
- Нажмите Enter или кликните вне поля для сохранения
- Нажмите Esc для отмены

### Удаление закладки

- Наведите курсор на закладку
- Кликните на кнопку "✕" справа
- Закладка будет удалена

## Особенности реализации

1. **Автосохранение**: Все изменения автоматически сохраняются в конфиг файл
2. **Дублирование**: Нельзя добавить папку, которая уже есть в закладках
3. **Только папки**: Можно добавить в закладки только папки (не файлы)
4. **Умное автодополнение**: Если имя не указано, используется имя папки
5. **Визуальная индикация**: Золотая звезда ⭐ показывает, что текущая папка в избранном
6. **Уведомления**: Toast-уведомления при успешном добавлении/удалении
7. **Три способа добавления**: Кнопка, контекстное меню, горячая клавиша

## Структура данных

### Bookmark
```typescript
interface Bookmark {
  id: string;           // Уникальный ID (bookmark_timestamp)
  name: string;         // Отображаемое имя
  path: string;         // Полный путь к папке
  created_at: number;   // Unix timestamp создания
}
```

### Config файл
```json
{
  "filesystem_backend": "real",
  "show_hidden_files": false,
  "default_view_mode": "grid",
  "theme": "luna",
  "bookmarks": [
    {
      "id": "bookmark_1234567890",
      "name": "My Project",
      "path": "/Users/username/projects/my-project",
      "created_at": 1234567890
    }
  ]
}
```

## ✅ Все реализованные возможности

- [x] Backend хранилище (JSON config)
- [x] API для добавления/удаления/переименования
- [x] Отображение в Sidebar
- [x] Кнопка-звездочка в Toolbar
- [x] Контекстное меню "Add to Favorites"
- [x] Горячая клавиша Ctrl+D
- [x] Inline переименование (двойной клик)
- [x] Удаление закладки (кнопка ✕)
- [x] Уведомления об успехе/ошибке
- [x] Визуальная индикация текущего статуса

## Потенциальные улучшения

- [ ] Иконки для разных типов папок
- [ ] Сортировка закладок (drag to reorder)
- [ ] Группировка закладок по категориям
- [ ] Цифровые клавиши для быстрого доступа (1-9)
- [ ] Экспорт/импорт закладок
- [ ] Drag & drop для добавления в избранное (было удалено из-за нестабильности)
