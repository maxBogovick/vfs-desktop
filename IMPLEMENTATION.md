# Реализация файловых операций и Tauri Backend

## 🎉 Что было реализовано

### 1. **Tauri Backend (Rust)**

Создан полноценный файловый backend в `src-tauri/src/commands.rs`:

#### Команды:

- ✅ **read_directory** - чтение содержимого директории
- ✅ **delete_item** - удаление файлов и папок
- ✅ **rename_item** - переименование файлов/папок
- ✅ **create_folder** - создание новой папки
- ✅ **copy_items** - копирование файлов/папок (поддержка множественного выбора)
- ✅ **move_items** - перемещение файлов/папок
- ✅ **get_home_directory** - получение домашней директории
- ✅ **get_file_info** - получение информации о файле

#### Функционал:

- Рекурсивное копирование директорий
- Автоматическая сортировка (папки первыми, затем по имени)
- Скрытие hidden файлов (начинающихся с точки)
- Полные метаданные (размер, дата создания/модификации/доступа)
- Обработка ошибок с понятными сообщениями

### 2. **Frontend (TypeScript/Vue)**

#### Обновлен `useFileSystem.ts`:

- ✅ Убраны mock данные
- ✅ Реальная интеграция с Tauri API
- ✅ Все файловые операции работают с реальной ФС
- ✅ Конвертация Unix timestamps в человекочитаемый формат
- ✅ Определение типа файла по расширению

#### Создан `useClipboard.ts`:

- ✅ Управление буфером обмена
- ✅ Copy операция
- ✅ Cut операция
- ✅ Paste операция
- ✅ Индикация типа операции (copy/cut)
- ✅ Автоочистка после cut + paste

#### Обновлен `App.vue`:

- ✅ Интеграция clipboard operations
- ✅ Реализованы все файловые операции:
  - Copy (Ctrl+C)
  - Cut (Ctrl+X)
  - Paste (Ctrl+V)
  - Delete (Delete key)
  - Rename (F2)
  - New Folder (Ctrl+Shift+N)
- ✅ Context Menu подключен к реальным операциям
- ✅ Горячие клавиши работают
- ✅ Автообновление директории после операций

## 🚀 Как использовать

### Запуск приложения:

```bash
npm run tauri dev
```

### Горячие клавиши:

| Клавиша | Действие |
|---------|----------|
| `Ctrl+C` | Скопировать выбранные файлы |
| `Ctrl+X` | Вырезать выбранные файлы |
| `Ctrl+V` | Вставить файлы |
| `Delete` | Удалить выбранные файлы |
| `F2` | Переименовать файл |
| `Ctrl+Shift+N` | Создать новую папку |
| `Ctrl+K` | Command Palette |
| `Ctrl+A` | Выделить все |
| `Ctrl+T` | Новая вкладка |
| `Backspace` | На уровень выше |

### Context Menu:

Правый клик на файле/папке открывает меню с опциями:
- Open
- Copy
- Cut
- Paste
- Rename
- Delete
- Properties

### Drag & Drop:

- Перетаскивание файлов между папками
- **Обычное перетаскивание** = перемещение
- **Ctrl + перетаскивание** = копирование
- Визуальная индикация цели drop

## 📋 Что работает:

✅ Реальная файловая система (не mock данные)
✅ Навигация по директориям
✅ Копирование файлов/папок
✅ Перемещение файлов/папок
✅ Удаление файлов/папок (с подтверждением)
✅ Переименование
✅ Создание папок
✅ Drag & Drop
✅ Clipboard (Copy/Cut/Paste)
✅ Множественное выделение
✅ Горячие клавиши
✅ Мультитабы
✅ История навигации (Back/Forward)
✅ Grid/List режимы отображения
✅ Preview панель
✅ Метаданные файлов (размер, даты)

## 🔧 Технические детали

### Архитектура:

```
Frontend (Vue/TS)
    ↓
useFileSystem composable
    ↓
Tauri invoke()
    ↓
Rust commands.rs
    ↓
macOS File System
```

### Безопасность:

- Подтверждение перед удалением
- Проверка существования файлов/путей
- Обработка ошибок на всех уровнях
- Валидация входных данных

### Производительность:

- Асинхронные операции
- Автоматическая сортировка на backend
- Эффективное копирование директорий
- Lazy loading метаданных

## 🐛 Известные ограничения:

1. Скрыты файлы начинающиеся с точки (можно изменить в `commands.rs`)
2. Нет поддержки символических ссылок
3. Нет индикации прогресса для больших файлов
4. Properties (свойства файла) пока только заглушка

## 📝 TODO для дальнейшей разработки:

- [ ] Прогресс бар для копирования больших файлов
- [ ] Символические ссылки
- [ ] Настройка отображения скрытых файлов
- [ ] Полноценный Properties dialog
- [ ]Undo/Redo операций
- [ ] Поиск внутри файлов
- [ ] Архивирование файлов
- [ ] Открытие файлов в системных приложениях
- [ ] Превью для изображений и PDF
- [ ] Drag & Drop из внешних источников

## 🎨 Windows XP стиль

Сохранен полный аутентичный дизайн Windows XP:
- Классическая Luna тема
- Правильные цвета и градиенты
- XP-стиль кнопок и меню
- Оригинальные иконки (emoji-версии)
- Знакомые диалоги и уведомления

## 🔗 Файлы изменены:

### Backend:
- `src-tauri/src/commands.rs` - новый модуль
- `src-tauri/src/lib.rs` - регистрация команд
- `src-tauri/Cargo.toml` - добавлена зависимость `dirs`

### Frontend:
- `src/composables/useFileSystem.ts` - переписан для Tauri API
- `src/composables/useClipboard.ts` - новый composable
- `src/App.vue` - добавлены file operations

Всё готово к использованию! 🎉
